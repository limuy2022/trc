编程工具：

1.遍历文件
2.文件路径
3.树节点
4.文件读取
5.类型检测
6.检查元素是否在容器内
7.获取元素在容器中的索引值
8.任意类型的转换
9.内存池（所有对象享有）
10.对象池（虚拟机对象专有）

编程规范：
1.数据集合使用struct，复杂对象使用class

设计理念：
1.能用char就不用string
2.能用指针就不用迭代器
3.能用数组就不用vector
4.能使用C语言特性就不使用c++特性
5.功能简洁，绝不冗余
6.能不用模板就不用模板

最后的提示：该项目代码并不复杂，层次也比较清楚，希望不要在未理解代码的情况下盲目改动！
