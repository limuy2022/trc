# calculate a^b
func fastpow(a: int, b: int) int {
  if b == 0 {
    return 1
  }
  if b == 1 {
    return a
  }
  tmp := fastpow(a, b / 2);
  tmp = tmp * tmp;
  if b % 2 != 0 {
    tmp = tmp * a
  }
  return tmp;
}
