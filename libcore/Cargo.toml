[package]
name = "libcore"
version = "0.1.0"
edition = "2021"

[dependencies]
rust-i18n = "3"
colored = "2"
downcast-rs = "1"
paste = "1"
collection_literals = "1"
derive = { path = "../derive" }
num_enum = "0.7.2"

[lib]
name = "libcore"
